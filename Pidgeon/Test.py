self.menuItems = [{
    'title': 'Start',
    'action': lambda: self.X },

    {'title': 'Shop',
     'action': lambda: self.shop},

    {'title': 'Quit',
    'action': lambda: self.exit_game()
    }]